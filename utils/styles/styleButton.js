import { StyleSheet } from "react-native";
import colors1 from "../colors/colors1";

const styleButton = StyleSheet.create({
    buttonLong : {
        backgroundColor: colors1.buttonColor,
        paddingVertical: 10,
        paddingHorizontal: 20,
        borderRadius: 5,
        marginTop: 10,
        alignItems: 'center',
    },
    
})

export default styleButton;